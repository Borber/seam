[package]
name = "seam_core_marcos"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
quote = "1"
syn = { version = "2", features = ['full'] }
proc-macro2 = "1"
regex = "1"

[lib]
proc-macro = true
